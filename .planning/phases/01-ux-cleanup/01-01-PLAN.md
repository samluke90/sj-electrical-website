---
phase: 01-ux-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/effects.js
  - css/styles.css
autonomous: true

must_haves:
  truths:
    - "User sees no spark/particle effects following their cursor"
    - "User clicking anywhere produces no electric arc effect"
    - "Light switch toggle still produces spark burst on click"
  artifacts:
    - path: "js/effects.js"
      provides: "Effects without spark cursor"
      contains: "createSparkBurst"
      not_contains: "initSparkCursor"
    - path: "css/styles.css"
      provides: "Styles without spark cursor CSS"
      not_contains: "@keyframes sparkMove1"
  key_links:
    - from: "js/effects.js initLightSwitch()"
      to: "createSparkBurst()"
      via: "function call on toggle click"
      pattern: "createSparkBurst\\(rect\\.left"
---

<objective>
Remove the spark cursor effect while preserving the spark burst for light switch toggle.

Purpose: The spark cursor effect (particles following mouse movement, electric arcs on click) creates visual noise and detracts from a professional feel. The light switch spark burst is a deliberate interaction and should remain.

Output: Clean effects.js and styles.css with spark cursor removed but createSparkBurst() intact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-ux-cleanup/01-CONTEXT.md
@.planning/phases/01-ux-cleanup/01-RESEARCH.md
@js/effects.js
@css/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove spark cursor JavaScript</name>
  <files>js/effects.js</files>
  <action>
Remove the spark cursor functionality from effects.js:

1. Remove the `initSparkCursor()` call from DOMContentLoaded (line 11)

2. Remove the entire `initSparkCursor()` function (lines 198-236) including:
   - mousemove event listener that creates sparks
   - click event listener that creates spark bursts and electric arcs on general clicks

3. Remove `createSpark(x, y)` function (lines 239-275) - only used by cursor movement

4. Remove `createElectricArc(x, y)` function (lines 319-342) - only used by cursor click

5. Remove the dynamic sparkMoveRandom keyframes injection at end of file (lines 345-358)

KEEP these functions (used by light switch and voltage meter):
- `createSparkBurst(x, y)` (lines 278-316) - used by light switch toggle and voltage meter
- The light switch click handler that calls createSparkBurst (line 166)
  </action>
  <verify>
1. Search effects.js for "initSparkCursor" - should find 0 matches
2. Search effects.js for "createSpark(" (without Burst) - should find 0 matches
3. Search effects.js for "createElectricArc" - should find 0 matches
4. Search effects.js for "sparkMoveRandom" - should find 0 matches
5. Search effects.js for "createSparkBurst" - should find matches (function still exists)
  </verify>
  <done>
initSparkCursor, createSpark, createElectricArc, and sparkMoveRandom removed. createSparkBurst preserved and still called by light switch toggle.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove spark cursor CSS</name>
  <files>css/styles.css</files>
  <action>
Remove spark cursor CSS from styles.css (lines 1746-1842):

1. Remove the entire "Spark Cursor Effect" section including:
   - `.spark` class (lines 1749-1754)
   - `.spark-particle` class (lines 1756-1763)
   - `@keyframes sparkFade` (lines 1765-1774)
   - `@keyframes sparkMove1` through `sparkMove5` (lines 1776-1799)
   - `.spark-particle:nth-child()` rules (lines 1801-1805)
   - `.electric-arc` class (lines 1808-1816)
   - `.arc-line` class (lines 1818-1827)
   - `@keyframes arcBurst` (lines 1829-1842)

KEEP the comment header for the next section (Lightbulb Loading Animation starts at line 1844).

Note: The .spark and .spark-particle classes ARE still used by createSparkBurst(), but createSparkBurst uses the Web Animations API (element.animate()) instead of CSS keyframes. The CSS is only needed for the cursor trail effect which we're removing. The burst effect is self-contained with inline animations.
  </action>
  <verify>
1. Search styles.css for "Spark Cursor Effect" comment - should find 0 matches
2. Search styles.css for "@keyframes sparkMove" - should find 0 matches
3. Search styles.css for ".electric-arc" - should find 0 matches
4. Search styles.css for "Lightbulb Loading Animation" - should still find 1 match (next section preserved)
  </verify>
  <done>
All spark cursor CSS removed. Lightbulb loading animation section intact.
  </done>
</task>

</tasks>

<verification>
1. Open the site in browser
2. Move mouse around - no spark particles should appear
3. Click anywhere on page - no electric arc should appear
4. Click the light switch toggle - spark burst SHOULD still appear around the switch
5. No JavaScript errors in console
</verification>

<success_criteria>
- UXFX-01 satisfied: No spark/particle cursor effects
- Light switch interaction preserved with spark burst
- No console errors or visual regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-ux-cleanup/01-01-SUMMARY.md`
</output>
