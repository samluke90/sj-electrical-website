---
phase: 03-content-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - services.html
  - quote.html
  - about.html
  - contact.html
  - css/styles.css
autonomous: true

must_haves:
  truths:
    - "Footer on all pages has social media links section"
    - "Social media links use placeholder hrefs (#) with aria-labels"
    - "Facebook and Instagram icons are present with proper SVG icons"
  artifacts:
    - path: "index.html"
      provides: "Social media placeholder links in footer"
      contains: "footer-social"
    - path: "css/styles.css"
      provides: "Social link styling"
      contains: ".social-link"
  key_links:
    - from: "footer-social section"
      to: "all page footers"
      via: "consistent HTML across all 5 pages"
      pattern: "class=\"footer-social\""
---

<objective>
Add social media placeholder links to all page footers, completing the structural elements needed for CONT-07.

Purpose: Prepare the site structure for future social media links while maintaining visual consistency.

Output: All 5 pages have footer social media section with Facebook and Instagram placeholders, styled consistently with the design system.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-update/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add social media CSS styles</name>
  <files>css/styles.css</files>
  <action>
    Add social media link styles to the footer section of styles.css (around line 880, after existing footer styles).

    Add these styles:
    ```css
    /* Social Media Links */
    .footer-social {
        margin-top: 20px;
    }

    .footer-social h4 {
        color: var(--white);
        margin-bottom: 15px;
        font-size: 1.1rem;
    }

    .social-links {
        display: flex;
        gap: 15px;
    }

    .social-link {
        width: 40px;
        height: 40px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: var(--radius);
        display: flex;
        align-items: center;
        justify-content: center;
        color: rgba(255, 255, 255, 0.7);
        transition: var(--transition);
    }

    .social-link:hover {
        background: var(--color-accent);
        color: var(--color-accent-text);
        transform: translateY(-2px);
    }

    .social-link svg {
        width: 20px;
        height: 20px;
    }

    /* Placeholder indicator for social links */
    .social-link[data-placeholder="true"] {
        opacity: 0.6;
    }

    .social-link[data-placeholder="true"]:hover {
        opacity: 1;
    }
    ```

    Place these styles in the footer section, before the responsive design section.
  </action>
  <verify>
    Check css/styles.css contains:
    - `.footer-social` class
    - `.social-links` class
    - `.social-link` class with hover states
  </verify>
  <done>
    - Social link styles added to stylesheet
    - Hover effects match orange accent design system
    - Placeholder opacity indicator included
  </done>
</task>

<task type="auto">
  <name>Task 2: Add social media HTML to all footers</name>
  <files>index.html, services.html, quote.html, about.html, contact.html</files>
  <action>
    In each of the 5 HTML files, add a social media section to the footer.

    Location: Inside the footer-grid, after the footer-contact div (before the closing `</div>` of footer-grid).

    Add this HTML block to each file's footer:
    ```html
    <div class="footer-social">
        <h4>Follow Us</h4>
        <div class="social-links">
            <a href="#" class="social-link" aria-label="Facebook" data-placeholder="true">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z"></path>
                </svg>
            </a>
            <a href="#" class="social-link" aria-label="Instagram" data-placeholder="true">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="2" y="2" width="20" height="20" rx="5" ry="5"></rect>
                    <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"></path>
                    <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                </svg>
            </a>
        </div>
    </div>
    ```

    The footer-grid structure should now be:
    1. footer-brand
    2. footer-nav
    3. footer-services
    4. footer-contact
    5. footer-social (new)

    Note: The footer-grid CSS uses `grid-template-columns: 2fr 1fr 1fr 1fr`. This may need adjustment to accommodate 5 columns. However, the social section is small and can flow naturally. If needed, adjust the grid or place social links within footer-brand.

    Alternative placement (simpler): Add the social-links div inside footer-brand, after the paragraph:
    ```html
    <div class="footer-brand">
        <a href="index.html" class="logo">...</a>
        <p>Professional, reliable electrical services...</p>
        <div class="social-links" style="margin-top: 15px;">
            <!-- social links here -->
        </div>
    </div>
    ```

    Use the simpler placement (inside footer-brand) to avoid grid layout changes.
  </action>
  <verify>
    For each of the 5 HTML files:
    - Search for "footer-social" or "social-links" in footer
    - Verify Facebook and Instagram links present
    - Verify data-placeholder="true" attributes
    - Verify aria-labels for accessibility
  </verify>
  <done>
    - All 5 pages have social media links in footer
    - Links have placeholder hrefs (#)
    - Links marked with data-placeholder for future replacement
    - Accessible with aria-labels
  </done>
</task>

<task type="auto">
  <name>Task 3: Update footer grid for 5 columns (if needed)</name>
  <files>css/styles.css</files>
  <action>
    If social media was added as a 5th grid item (not inside footer-brand), update the footer-grid CSS:

    Find the `.footer-grid` rule (around line 818) and update:
    ```css
    .footer-grid {
        display: grid;
        grid-template-columns: 2fr 1fr 1fr 1fr 1fr;
        gap: 40px;
        margin-bottom: 40px;
    }
    ```

    Also update the responsive breakpoint for tablet (around line 1543-1545):
    ```css
    @media (max-width: 991px) {
        .footer-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }
    ```

    And mobile (around line 1585):
    ```css
    @media (max-width: 767px) {
        .footer-grid {
            grid-template-columns: 1fr;
        }
    }
    ```

    These responsive rules should already handle the new column gracefully.

    **If social was placed inside footer-brand:** Skip this task - no grid changes needed.
  </action>
  <verify>
    Open site in browser at different widths:
    - Desktop: Footer grid displays properly with social section
    - Tablet (991px): Grid reflows to 2 columns
    - Mobile (767px): Grid reflows to 1 column
  </verify>
  <done>
    - Footer layout works at all breakpoints
    - Social links visible and accessible on all devices
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **CSS check:**
   - Open css/styles.css
   - Verify .social-link styles exist
   - Verify hover effects defined

2. **HTML check (all 5 pages):**
   - Each footer has social links section
   - Links point to # (placeholder)
   - Links have data-placeholder="true"
   - Links have aria-labels

3. **Visual check:**
   - Open index.html in browser
   - Scroll to footer
   - Verify social icons visible
   - Hover over icons - orange background appears
   - Check mobile view - icons still visible

4. **Responsive check:**
   - Resize browser to mobile width
   - Footer still readable and functional
</verification>

<success_criteria>
- CONT-07 (social media placeholder links) complete
- All 5 pages have consistent footer social section
- Social links styled with orange hover effect
- Links marked as placeholders for future update
- Responsive design maintained
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-update/03-02-SUMMARY.md`
</output>
