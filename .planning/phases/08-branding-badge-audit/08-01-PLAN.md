---
phase: 08-branding-badge-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
  - index.html
  - about.html
autonomous: true

must_haves:
  truths:
    - "NICEIC badge displays as rectangular image in all footers (not circular)"
    - "NICEIC badge visible as actual image in index page credentials section"
    - "NICEIC badge visible as actual image in about page credentials section"
    - "Header logo displays consistently on all 5 pages (verified working)"
    - "Footer logo displays consistently on all 5 pages (verified working)"
  artifacts:
    - path: "css/styles.css"
      provides: "Distinct styling for footer badge vs credentials badge"
      contains: ".niceic-styled-badge"
    - path: "index.html"
      provides: "Credentials section with actual NICEIC badge image"
      contains: "niceic-badge.jpg"
    - path: "about.html"
      provides: "Credentials section with actual NICEIC badge image"
      contains: "niceic-badge.jpg"
  key_links:
    - from: "css/styles.css"
      to: "footer .niceic-badge"
      via: "class selector"
      pattern: "\\.niceic-badge.*height: 60px"
    - from: "index.html credentials-card"
      to: "assets/images/niceic-badge.jpg"
      via: "img src"
      pattern: "credentials-card.*niceic-badge\\.jpg"
---

<objective>
Fix NICEIC badge display across all pages by resolving CSS class conflict and replacing styled text with actual badge image in credentials sections.

Purpose: Ensure NICEIC certification badge displays correctly as a rectangular professional image (not circular styled text) to build customer trust.
Output: Fixed CSS with distinct class names, updated credentials sections using actual badge image.
</objective>

<execution_context>
@/home/samluke90/.claude/get-shit-done/workflows/execute-plan.md
@/home/samluke90/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@css/styles.css
@index.html
@about.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CSS class conflict for NICEIC badge</name>
  <files>css/styles.css</files>
  <action>
    Rename the credentials-card styled badge class from `.niceic-badge` to `.niceic-styled-badge` to avoid overriding the footer badge styling.

    Currently there are TWO `.niceic-badge` definitions:
    - Lines 273-277: Footer badge (height: 60px, width: auto, border-radius: 4px) - KEEP THIS
    - Lines 640-663: Credentials styled text badge (circular, 150px, border-radius: 50%) - RENAME THIS

    Change ONLY the second definition (around line 640):
    - `.niceic-badge` -> `.niceic-styled-badge`
    - `.niceic-badge span:first-child` -> `.niceic-styled-badge span:first-child`
    - `.niceic-badge span:last-child` -> `.niceic-styled-badge span:last-child`

    Do NOT modify the first definition (lines 273-277) - this is the correct footer styling.
  </action>
  <verify>
    Grep css/styles.css for "niceic-badge" - should find:
    1. One `.niceic-badge` around line 273 (footer styling, unchanged)
    2. One `.niceic-styled-badge` around line 640 (credentials styling, renamed)
    No duplicate `.niceic-badge` selectors for the circular style.
  </verify>
  <done>
    Footer `.niceic-badge` class is distinct from credentials `.niceic-styled-badge` class. No CSS conflict.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace styled text with actual badge image in credentials sections</name>
  <files>index.html, about.html</files>
  <action>
    Update credentials sections to use the actual NICEIC badge image instead of styled text.

    **In index.html (around lines 280-287):**
    Replace:
    ```html
    <div class="credentials-card">
        <div class="niceic-badge">
            <span>NICEIC</span>
            <span>APPROVED</span>
        </div>
        <h3>Certified Excellence</h3>
        <p>All our work meets the strictest safety standards...</p>
    </div>
    ```

    With:
    ```html
    <div class="credentials-card">
        <img src="assets/images/niceic-badge.jpg" alt="NICEIC Approved Contractor" class="credentials-badge-img">
        <h3>Certified Excellence</h3>
        <p>All our work meets the strictest safety standards and comes with full certification and guarantees.</p>
    </div>
    ```

    **In about.html (around lines 104-111):**
    Replace:
    ```html
    <div class="about-badge-card">
        <div class="niceic-badge">
            <span>NICEIC</span>
            <span>APPROVED</span>
        </div>
        <h3>Certified Excellence</h3>
        <p>NICEIC Approved Contractor status means...</p>
    </div>
    ```

    With:
    ```html
    <div class="about-badge-card">
        <img src="assets/images/niceic-badge.jpg" alt="NICEIC Approved Contractor" class="credentials-badge-img">
        <h3>Certified Excellence</h3>
        <p>NICEIC Approved Contractor status means our work is regularly assessed against rigorous technical and safety standards. You can trust that every installation meets or exceeds current regulations.</p>
    </div>
    ```

    **Add CSS for new class** in css/styles.css (after the credentials-card styles around line 673):
    ```css
    .credentials-badge-img {
        width: 120px;
        height: auto;
        margin: 0 auto 20px;
        display: block;
        border-radius: 4px;
    }
    ```

    Use `credentials-badge-img` class (not `niceic-badge`) to avoid inheriting footer badge sizing.
  </action>
  <verify>
    1. Grep index.html for "niceic-badge.jpg" in credentials section - should find img tag
    2. Grep about.html for "niceic-badge.jpg" in credentials section - should find img tag
    3. Grep css/styles.css for "credentials-badge-img" - should find new styling
    4. Open index.html and about.html in browser - credentials sections show actual NICEIC badge image
    5. Check all 5 footers - NICEIC badge displays as rectangular (not circular)
  </verify>
  <done>
    - index.html credentials section displays actual NICEIC badge image
    - about.html credentials section displays actual NICEIC badge image
    - Footer NICEIC badges on all 5 pages display as rectangular images
    - No more circular styled text badges in credentials sections
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify logo consistency across all 5 pages</name>
  <files>index.html, about.html, services.html, quote.html, contact.html</files>
  <action>
    Verify that header and footer logos display consistently across all 5 pages (BRAND-01, BRAND-02).

    Logos were updated in a previous session (2026-02-01) and should already be working:
    - Header: Uses `logo-white.png` (dark theme) and `logo-horizontal.png` (light theme) with theme toggle
    - Footer: Uses `logo-white.png`

    **Verification steps:**
    1. Check each HTML file has consistent header logo markup:
       - Dark theme: `assets/images/logo-white.png`
       - Light theme: `assets/images/logo-horizontal.png`
       - Both with appropriate alt text and class

    2. Check each HTML file has consistent footer logo markup:
       - `assets/images/logo-white.png`
       - With appropriate alt text and sizing

    3. Confirm logo files exist in assets/images/ directory

    **If any inconsistency found:** Document what needs fixing. The expectation is logos ARE consistent (already implemented).

    **If all consistent:** Document verification passed with specific evidence.
  </action>
  <verify>
    Run grep across all 5 HTML files to confirm logo references:
    ```bash
    grep -l "logo-white.png" index.html about.html services.html quote.html contact.html
    grep -l "logo-horizontal.png" index.html about.html services.html quote.html contact.html
    ```
    Both commands should return all 5 files.

    Verify logo files exist:
    ```bash
    ls -la assets/images/logo-white.png assets/images/logo-horizontal.png
    ```
  </verify>
  <done>
    - Header logo displays consistently on all 5 pages (logo-white.png for dark, logo-horizontal.png for light)
    - Footer logo displays consistently on all 5 pages (logo-white.png)
    - BRAND-01 verified: Header logos consistent
    - BRAND-02 verified: Footer logos consistent
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **CSS class separation:**
   ```bash
   grep -n "\.niceic-badge" css/styles.css
   grep -n "\.niceic-styled-badge" css/styles.css
   grep -n "\.credentials-badge-img" css/styles.css
   ```
   Should show distinct classes with no conflicts.

2. **Credentials sections use actual images:**
   ```bash
   grep -A2 "credentials-card\|about-badge-card" index.html about.html | grep "niceic-badge.jpg"
   ```
   Should show img tags with niceic-badge.jpg in both files.

3. **Visual verification:**
   Open each page in browser and verify:
   - index.html: Hero/credentials section shows rectangular NICEIC badge image
   - about.html: Credentials section shows rectangular NICEIC badge image
   - All 5 pages: Footer shows rectangular NICEIC badge (not circular)

4. **Logo consistency (BRAND-01, BRAND-02):**
   ```bash
   for f in index.html about.html services.html quote.html contact.html; do
     echo "=== $f ===" && grep -c "logo-white.png" $f && grep -c "logo-horizontal.png" $f
   done
   ```
   Each page should have both logo references (header uses both for theme toggle, footer uses white).
</verification>

<success_criteria>
1. NICEIC badge displays as rectangular image in all 5 footers
2. NICEIC badge visible as actual image (not styled text) in index.html credentials section
3. NICEIC badge visible as actual image (not styled text) in about.html credentials section
4. BRAND-01: Header logo displays consistently on all 5 pages (verified with grep + visual check)
5. BRAND-02: Footer logo displays consistently on all 5 pages (verified with grep + visual check)
6. No CSS class conflicts between footer and credentials badge styles
</success_criteria>

<output>
After completion, create `.planning/phases/08-branding-badge-audit/08-01-SUMMARY.md`
</output>
