---
phase: 09-contact-form-fix
plan: 01
type: execute
wave: 1
depends_on:
  - 08-branding-badge-audit
files_modified:
  - contact.html
  - css/styles.css
  - js/contact-form.js
autonomous: true

must_haves:
  truths:
    - "Contact form posts to receptionist/n8n endpoint using FormData, matching the working quote form pattern"
    - "No JSON Content-Type preflight on contact form submission"
    - "User sees accessible success confirmation after successful submission"
    - "User sees accessible error message on failed submission"
    - "Form fields reset after successful submission"
  artifacts:
    - path: "contact.html"
      provides: "Contact form error/success containers, honeypot, multipart form setup, external script include"
      contains: "contactFormSuccess"
    - path: "js/contact-form.js"
      provides: "Contact form submission handler"
      contains: "new FormData(contactForm)"
    - path: "css/styles.css"
      provides: "Contact success message styling"
      contains: ".contact-form-success"
---

<objective>
Fix the general contact form so it uses the same reliable webhook submission pattern as the quote form, with proper user feedback and no fragile JSON response assumption.

Purpose: Make the Contact page lead path actually usable for non-quote enquiries.
Output: Functional contact form with FormData POST, loading state, accessible success/error UI, honeypot, and reset-on-success behaviour.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@contact.html
@css/styles.css
@js/quote-form.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline JSON handler with FormData submission</name>
  <files>contact.html, js/contact-form.js</files>
  <action>
    Move contact form behaviour out of inline script and into js/contact-form.js.

    Use FormData, not JSON, so the browser does not set application/json or trigger a fragile CORS preflight. Treat any response.ok result as success, matching the quote form behaviour.
  </action>
  <verify>
    - node --check js/contact-form.js
    - Browser/CDP test: fetch is called with body instanceof FormData and no explicit headers
  </verify>
  <done>
    Contact form posts FormData to https://receptionist.jenk0.uk/api/contact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add accessible success and error feedback</name>
  <files>contact.html, css/styles.css, js/contact-form.js</files>
  <action>
    Add role="alert" error container and role="status" success container. Replace alert() popups with inline page feedback and loading state.
  </action>
  <verify>
    Browser/CDP success-path test:
    - success container gains .show
    - error container remains hidden
    - fields reset
    - submit button aria-busy returns to false

    Browser/CDP failure-path test:
    - error container gains .visible
    - success remains hidden
    - fields are preserved for retry
    - submit button aria-busy returns to false
  </verify>
  <done>
    Contact form gives clear success/error feedback without browser alert popups.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add basic spam protection and planning cleanup</name>
  <files>contact.html, .planning/todos/*, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Add hidden honeypot field matching quote form convention. Archive the stale logo audit todo now consumed by v1.3 Phase 8. Update roadmap/state once verified.
  </action>
  <verify>
    - Honeypot appears in submitted FormData as empty _honeypot
    - .planning/todos/pending is empty
    - ROADMAP and STATE show v1.3 complete
  </verify>
  <done>
    v1.3 planning state reflects completed branding and contact form work.
  </done>
</task>

</tasks>

<verification>
Run:

```bash
node --check js/contact-form.js
python3 - <<'PY'
from html.parser import HTMLParser
from pathlib import Path
class P(HTMLParser): pass
for f in ['contact.html','index.html','about.html','services.html','quote.html']:
    p=P(); p.feed(Path(f).read_text()); p.close(); print(f, 'ok')
PY
```

Then run headless Chromium/CDP tests for success and failure paths with fetch stubbed locally so no real customer message is sent.
</verification>
